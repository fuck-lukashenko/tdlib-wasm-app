import React from 'react';

const App = () => (
  <div style={{ marginTop: '20px', textAlign: 'center' }}>
    <p>You can experiment with the TDLIB client through the Dev tools.</p>
    <p>There is a pre-initialized <code style={{ background: 'lightpink', borderRadius: '3px', padding: '3px 6px' }}>TelegramClient</code> available at the console.</p>
  </div>
);

export default App;
